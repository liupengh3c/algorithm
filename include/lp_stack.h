#pragma once

#include <vector>

using namespace std;

class lp_stack
{
private:
    /* data */
public:
    lp_stack(/* args */);
    ~lp_stack();

    int trap(vector<int> &height);
};

lp_stack::lp_stack(/* args */)
{
}

lp_stack::~lp_stack()
{
}
