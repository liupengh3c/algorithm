#pragma once

#include <vector>

using namespace std;

class lp_array
{
private:
    /* data */
public:
    lp_array(/* args */);
    ~lp_array();
    int removeDuplicates(vector<int> &nums);
};

lp_array::lp_array(/* args */)
{
}

lp_array::~lp_array()
{
}
